<?php
/* Legacy old-site activity brochure page — retired at the new-site cutover. */
/* 301 to the closest live equivalent (the Experiences hub). Clean URL = single hop. */
header('Location: /activities', true, 301);
exit;
